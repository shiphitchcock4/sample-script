language: node_js   

# Version number
node_js:
  - 0.12
  - 0.10
 
env: 
  global:
    - FOO=BOO
   # - COMMITER=VARSHA
  
build:
  ci:
    - node --version
    - echo $PULL_REQUEST_REPO_FULL_NAME 
    - echo "hello world"
    - echo "$COMMITER"
    - echo "$foo"  
